# 🚀 快速开始指南

## ✅ 当前状态

- ✅ 项目框架已搭建完成
- ✅ API Key 已配置并测试通过
- ✅ 所有依赖已安装

## 📝 使用步骤

### 方式 1: 分析已有论文（推荐）

如果您已经有论文文件（PDF 格式），直接运行：

```bash
# 完整流程：分析 + 内化 + 代码生成
python src/main.py full papers/your_paper.pdf
```

这将自动完成：
1. 解析论文（提取标题、作者、摘要等）
2. AI 深度分析（全面分析、关键点提取、实现指南）
3. 生成笔记（Markdown 格式）
4. 更新知识图谱
5. 生成代码项目框架

### 方式 2: 快速分析

如果只想快速了解论文内容：

```bash
# 生成摘要
python src/main.py analyze papers/your_paper.pdf --type summary

# 提取创新点
python src/main.py analyze papers/your_paper.pdf --type innovation

# 生成实现指南
python src/main.py analyze papers/your_paper.pdf --type implementation
```

### 方式 3: 获取 SIGGRAPH 信息

```bash
python src/main.py fetch
```

## 📁 准备论文文件

### 方式 1: 自动下载（推荐）

使用 arXiv ID 自动下载论文，系统会自动创建目录结构：

```bash
python src/main.py download https://arxiv.org/abs/2301.12345
```

或者使用 Python 代码：
```python
from src.paper.fetcher import PaperFetcher
fetcher = PaperFetcher()
paper_path = fetcher.download_arxiv_paper("2301.12345")
```

### 方式 2: 手动放置

1. 创建 `papers/<paper_id>/` 目录（使用论文ID作为文件夹名）
2. 将论文 PDF 文件重命名为 `paper.pdf` 并放入该目录
3. 运行分析命令

**目录结构示例**:
```
papers/
├── 2301.12345/
│   └── paper.pdf
├── 2302.12345/
│   └── paper.pdf
└── siggraph/
```

> **提示**: 每篇论文使用独立的子文件夹（`papers/<paper_id>/paper.pdf`），便于管理和维护多篇论文。

## 📊 输出文件位置

运行完整流程后，您会在以下位置找到生成的文件：

### 新结构（推荐，按论文ID组织）

如果论文文件名包含 arXiv ID（如 `2301.12345_论文标题.pdf`），所有输出会组织在：
- `data/papers/2301.12345/notes/` - 笔记文件
- `data/papers/2301.12345/code/` - 代码项目
- `data/papers/2301.12345/summaries/` - 摘要文件
- `data/papers/2301.12345/guides/` - 阅读指南（如果启用辅助阅读）

### 旧结构（兼容）

如果无法识别论文ID，文件会保存在：
- **笔记**: `data/notes/YYYYMMDD_HHMMSS_论文标题.md`
- **代码项目**: `data/code/论文标题/`
- **摘要**: `data/summaries/论文标题_summary.md`
- **实现指南**: `data/code/论文标题_implementation_guide.md`
- **知识图谱**: `data/knowledge/knowledge_graph.json`

## 🎯 下一步

1. **准备论文**: 将 PDF 文件放入 `papers/` 目录（建议文件名包含 arXiv ID）
2. **运行分析**: 使用 `python src/main.py full papers/your_paper.pdf` 命令
3. **查看结果**: 检查 `data/papers/<paper_id>/` 或 `data/` 目录下生成的笔记和代码框架
4. **开始实现**: 在生成的代码项目中实现论文算法

## 💡 提示

- 首次使用建议先用一篇短论文测试
- 长论文建议在 `config.yaml` 中使用 `moonshot-v1-128k` 模型
- 生成的代码框架需要根据论文内容进一步完善

## ❓ 需要帮助？

- 查看 [README.md](README.md) 了解完整功能
- 查看 [使用示例](docs/usage_examples.md) 查看详细示例
- 查看 [API 配置](docs/api_setup.md) 了解 API 配置
